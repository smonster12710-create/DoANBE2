services:
  # PHP Service
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: doanbe2-php
    container_name: doanbe2-app
    restart: unless-stopped
    tty: true
    environment:
      SERVICE_NAME: app
      SERVICE_TAGS: dev
    volumes:
      - ./:/var/www
    networks:
      - app-network

  # Nginx Service (Web Server)
  webserver:
    image: nginx:stable-alpine
    container_name: doanbe2-webserver
    restart: unless-stopped
    tty: true
    ports:
      - "8000:80"
    volumes:
      - ./:/var/www
      - ./docker-config/nginx:/etc/nginx/conf.d/:/etc/nginx/conf.d/
    networks:
      - app-network

  # MySQL Service (Database)
  db:
    image: mysql:8.0
    container_name: doanbe2-db
    restart: unless-stopped
    tty: true
    ports:
      - "3306:3306"
    environment:
      MYSQL_DATABASE: techstore_db
      MYSQL_ROOT_PASSWORD: root
      MYSQL_PASSWORD: root
      MYSQL_USER: user
    volumes:
      - dbdata:/var/lib/mysql
    networks:
      - app-network

  # phpMyAdmin Service
  phpmyadmin:
    image: phpmyadmin
    container_name: doanbe2-phpmyadmin
    restart: unless-stopped
    ports:
      - "8080:80"
    environment:
      - PMA_HOST=db
      - PMA_PORT=3306
      - ARBITRARY_SERVER_CONNECTION=1
    networks:
      - app-network

networks:
  app-network:
    driver: bridge

volumes:
  dbdata:
    driver: local